# adapt design system docs

Storybook em Vue 3 + Vite para documentar tokens (primitivas e semânticas).

## Scripts
- `npm run storybook` – ambiente de docs
- `npm run build-storybook` – build estático em `storybook-static` (para Vercel)
