import { Meta, Title, Subtitle } from '@storybook/addon-docs/blocks';
import { palettes, alphas } from '../../tokens/colors';

<Meta title="Cores/Cores primitivas" />

<Title>Cores primitivas</Title>
<Subtitle>
  Escalas base em cartões compactos, prontas para aplicação como tokens.
</Subtitle>

<div style={{ margin: '12px 0 16px', color: '#4a4a4a', lineHeight: 1.6 }}>
  Os tokens primitivos são criados a partir de uma paleta de cores-chave selecionada pelo time de design. Cada cor tem seu tom manipulado para gerar uma escala (geralmente 10 variações), que depois é associada a tokens semânticos do sistema.
</div>

{Object.entries(palettes).map(([id, scale]) => (
  <div key={id} style={{ marginBottom: '16px' }}>
    <h3
      style={{
        marginTop: '12px',
        marginBottom: '8px',
        fontSize: '16px',
        fontWeight: 700,
        textTransform: 'capitalize'
      }}
    >
      {id}
    </h3>
    <div
      style={{
        display: 'grid',
        gap: '10px',
        gridTemplateColumns: 'repeat(auto-fit, minmax(160px, 1fr))'
      }}
    >
      {Object.entries(scale).map(([tone, value]) => (
        <div
          key={`${id}-${tone}`}
          style={{
            border: '1px solid #e6e6e6',
            borderRadius: '12px',
            padding: '10px',
            display: 'grid',
            gap: '8px',
            alignItems: 'center',
            boxShadow: '0 4px 12px rgba(0,0,0,0.04)',
            background: '#ffffff'
          }}
        >
          <div
            style={{
              width: '100%',
              height: '44px',
              borderRadius: '10px',
              background: value,
              border: '1px solid rgba(0,0,0,0.04)'
            }}
          />
          <div
            style={{
              display: 'flex',
              justifyContent: 'space-between',
              alignItems: 'center'
            }}
          >
            <span
              style={{
                fontWeight: 600,
                textTransform: 'capitalize',
                fontSize: '13px'
              }}
            >{`${id}-${tone}`}</span>
            <span style={{ fontFamily: 'monospace', fontSize: '12px', color: '#555' }}>
              {value}
            </span>
          </div>
        </div>
      ))}
    </div>
  </div>
))}

<h3 style={{ marginTop: '28px', marginBottom: '8px', fontSize: '16px', fontWeight: 700 }}>
  Alphas — white
</h3>
<div
  style={{
    display: 'grid',
    gap: '10px',
    gridTemplateColumns: 'repeat(auto-fit, minmax(160px, 1fr))'
  }}
>
  {Object.entries(alphas)
    .filter(([token]) => token.startsWith('white'))
    .map(([token, value]) => (
      <div
        key={token}
        style={{
          border: '1px solid #e6e6e6',
          borderRadius: '12px',
          padding: '10px',
          display: 'grid',
          gap: '8px',
          alignItems: 'center',
          boxShadow: '0 4px 12px rgba(0,0,0,0.04)',
          background: '#ffffff'
        }}
      >
        <div
          style={{
            position: 'relative',
            width: '100%',
            height: '44px',
            borderRadius: '10px',
            overflow: 'hidden',
            border: '1px solid rgba(0,0,0,0.04)'
          }}
        >
          <div
            style={{
              position: 'absolute',
              inset: 0,
              backgroundImage:
                'linear-gradient(45deg, #e6e6e6 25%, transparent 25%, transparent 75%, #e6e6e6 75%), linear-gradient(45deg, #e6e6e6 25%, transparent 25%, transparent 75%, #e6e6e6 75%)',
              backgroundSize: '12px 12px',
              backgroundPosition: '0 0, 6px 6px'
            }}
          />
          <div
            style={{
              position: 'absolute',
              inset: 0,
              background: value
            }}
          />
        </div>
        <div style={{ display: 'grid', gap: '2px' }}>
          <div
            style={{
              fontWeight: 600,
              textTransform: 'lowercase',
              fontSize: '13px',
              lineHeight: 1.3
            }}
          >
            {token}
          </div>
          <div
            style={{
              fontFamily: 'monospace',
              fontSize: '11px',
              color: '#555',
              wordBreak: 'break-word',
              lineHeight: 1.3
            }}
          >
            {value}
          </div>
        </div>
      </div>
    ))}
</div>

<h3 style={{ marginTop: '28px', marginBottom: '8px', fontSize: '16px', fontWeight: 700 }}>
  Alphas — black
</h3>
<div
  style={{
    display: 'grid',
    gap: '10px',
    gridTemplateColumns: 'repeat(auto-fit, minmax(160px, 1fr))'
  }}
>
  {Object.entries(alphas)
    .filter(([token]) => token.startsWith('black'))
    .map(([token, value]) => (
      <div
        key={token}
        style={{
          border: '1px solid #e6e6e6',
          borderRadius: '12px',
          padding: '10px',
          display: 'grid',
          gap: '8px',
          alignItems: 'center',
          boxShadow: '0 4px 12px rgba(0,0,0,0.04)',
          background: '#ffffff'
        }}
      >
        <div
          style={{
            position: 'relative',
            width: '100%',
            height: '44px',
            borderRadius: '10px',
            overflow: 'hidden',
            border: '1px solid rgba(0,0,0,0.04)'
          }}
        >
          <div
            style={{
              position: 'absolute',
              inset: 0,
              backgroundImage:
                'linear-gradient(45deg, #e6e6e6 25%, transparent 25%, transparent 75%, #e6e6e6 75%), linear-gradient(45deg, #e6e6e6 25%, transparent 25%, transparent 75%, #e6e6e6 75%)',
              backgroundSize: '12px 12px',
              backgroundPosition: '0 0, 6px 6px'
            }}
          />
          <div
            style={{
              position: 'absolute',
              inset: 0,
              background: value
            }}
          />
        </div>
        <div>
          <div
            style={{
              fontWeight: 600,
              textTransform: 'lowercase',
              fontSize: '13px',
              lineHeight: 1.3
            }}
          >
            {token}
          </div>
          <div
            style={{
              fontFamily: 'monospace',
              fontSize: '11px',
              color: '#555',
              wordBreak: 'break-word',
              lineHeight: 1.3
            }}
          >
            {value}
          </div>
        </div>
      </div>
    ))}
</div>
